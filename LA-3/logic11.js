function calculateSI(principal, rate, time) {
    // Mathematical calculation in external function
    // Formula implementation: SI = (P x R x T) / 100
    var simpleInterest = (principal * rate * time) / 100;

    // Returning calculated result
    return simpleInterest;
}
